# Configure npm to handle optional dependencies better
optional=false
# Disable native module compilation warnings
rollup-skip-native=true
# Ensure consistent installs across platforms
package-lock=true 
workspaces=false