import React from 'react'

import FilterButton from './FilterIconButtons'

export default {
  component: FilterButton,
  title: 'Filter Button',
}

export const FilterButtonComponent = () => <FilterButton />
